# Screenshot Placeholder — fixtures/univabio-health

This fixture uses a mock `cleaned.json` for the health demo (CDC MedlinePlus-inspired "Understanding Diabetes").

**No static screenshot is bundled.** For the UnivaBio PDF and demo video, capture a live screenshot of the health page restructured in ReadEasy:

1. Run the app (`npm run dev`) and paste the cleaned text from `cleaned.json` via the Raw text tab, or host the URL if fetched live.
2. Capture the before/after view (cleaned original on left, restructured health page on right — plain-language sections + Action checklist).
3. Save as `public/fixtures/univabio-health.png` and reference it in the one-pager / video. Alternatively, screenshot live during recording — do not commit a placeholder image.

Existing trio fixtures (`fixtures/irs-eitc`, `fixtures/utdallas-first-year-apply`, `fixtures/uscis-students-employment`) remain the network-independent demo path; this health fixture follows the same pattern (`cleaned.json` mirrors `cleaned.txt`/`meta.json`) for a health-specific example.

Generated: 2026-10-01 (UnivaBio window). To be replaced with a live capture before PDF export.
