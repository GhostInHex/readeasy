"use client";

import ComingSoon from "@/components/modes/ComingSoon";
import type { ModeProps } from "@/components/modes/types";

/** Stub until ticket 07 (the interactive deadline checklist). */
export default function ActionMode(_props: ModeProps) {
  return <ComingSoon name="Action" />;
}
