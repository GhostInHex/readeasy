"use client";

import ComingSoon from "@/components/modes/ComingSoon";
import type { ModeProps } from "@/components/modes/types";

/** Stub until ticket 11 (micro-cards from the ADHD restructure variant). */
export default function AdhdMode(_props: ModeProps) {
  return <ComingSoon name="ADHD" />;
}
