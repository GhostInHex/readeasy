node_modules
.next
out
.env*.local
.vercel
*.tsbuildinfo

.pnpm-store/

.scratch/readeasy-git/
.env*

.worktrees/
.scratch/logs/

# Local build tooling (verify-trio stays: package.json's verify:trio needs it)
tools/*
!tools/verify-trio.ts
.scratch/

# Screenshots from a design-review round: regenerated on demand, never referenced by the app.
.impeccable/

# Demo-build intermediates and local logs
demo-output/tmp-build/
demo-output/**/*.log
devserver.log
npm-install.log
transform-*.json
